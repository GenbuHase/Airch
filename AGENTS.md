<!-- AUTO-GENERATED BY airch (https://github.com/GenbuHase/Airch) -->
<!-- DO NOT EDIT MANUALLY. Update airch.yaml and run 'airch generate' to sync. -->

# AI Agent Guidelines: airch

> Architecture Style: **modular-monorepo**
> Description: Architectural manifest & AI context generator for AI-driven development

You are an expert AI software engineer assisting in the development of this codebase. You MUST strictly adhere to the following architectural boundaries and project conventions at all times. Any code that violates forbidden imports or naming conventions will be rejected by CI.

## 1. Architectural Boundaries & Module Rules
### CLI Package (`packages/cli/src/**`)
CLI commands, reporters, and local UI server

- **Import Boundaries**:
  - ❌ **FORBIDDEN**: `packages/ui/src/**`
  - ⭕ **ALLOWED**: Any imports except forbidden ones

### Core Package (`packages/core/src/**`)
Core domain logic, schemas, parser, generator, and linter

- **Import Boundaries**:
  - ❌ **FORBIDDEN**: `packages/cli/**`, `packages/ui/**`
  - ⭕ **ALLOWED**: Any imports except forbidden ones

### UI Package (`packages/ui/src/**`)
Web dashboard frontend built with React and DaisyUI

- **Import Boundaries**:
  - ❌ **FORBIDDEN**: `packages/cli/src/**`
  - ⭕ **ALLOWED**: Any imports except forbidden ones

## 2. Coding Conventions & Standards
### TypeScript Standards
- **Strict Mode**: Enabled (`true`)
- **Export Style**: `named` export
- **Type Import Style**: `type-only`

### Architectural Principles & AI Instructions
- packages/core must remain standalone and never import from packages/cli or packages/ui
- packages/cli must not directly import internals of packages/ui
- packages/ui must not directly import internals of packages/cli
- All code must be strictly typed and unit tested

## 3. Verification & Execution Commands
### Executable Project Commands
Use these commands to verify, test, and build your changes:

- **`build`**: `pnpm -r run build` - Build all packages in monorepo
- **`check`**: `pnpm airch check --strict --format github` - Verify architectural boundaries
- **`test`**: `vitest run` - Run all test suites

## 4. Pre-Completion Self Checklist for AI
Before presenting your code to the user or finishing a task, verify:
1. [ ] Have I imported any modules from a `FORBIDDEN` path?
2. [ ] Do all newly created files follow the designated naming convention?
3. [ ] Are all required files present in any new module?
4. [ ] Did I run the verification commands (e.g. `airch check`) to guarantee boundary integrity?
